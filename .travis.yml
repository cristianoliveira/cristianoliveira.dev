language: node_js
node_js:
  - '8'
install:
  - npm install -g codecov
script:
  - yarn install
  - yarn test --runInBand --no-cache
  - sh scripts/publish_changes.sh
cache:
  directories:
    - node_modules
